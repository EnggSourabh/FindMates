# FindMate Backend Environment Variables

# The connection string to your MongoDB cluster/local instance
MONGO_URL=mongodb://localhost:27017

# The name of the database to use
DATABASE_NAME=FindMate

# The URI for MLflow tracking (e.g. sqlite:///mlflow.db or a remote server)
# MLFLOW_TRACKING_URI=
